# Дорожная карта проекта: Panama Runner

## ПРАВИЛА ДЛЯ ИИ-АССИСТЕНТА
1. Выполняем СТРОГО по одному пункту за раз (одна галочка = один промпт).
2. Не переходим к следующему шагу без явной команды "Работает, идем дальше".
3. Код пишем частями. Если нужно изменить существующий метод, выводим ТОЛЬКО его, а не весь класс.
4. НИКАКОГО рефакторинга работающего кода без прямого приказа.

---

## Фаза 1: Базовая настройка и Архитектура (Domain)
- [х] 1.1. Настроить `build.gradle` (добавить Compose, Coroutines, ViewModel, Firebase, Yandex Mobile Ads, ARCore).
- [х] 1.2. Создать структуру пакетов: `domain`, `presentation`, `data`, `di`.
- [х] 1.3. В `domain/models` создать `sealed class GameObject` (Panama, BadPanama). Задать параметры: x, y, радиус/размер, скорость падения.
- [х] 1.4. Создать `data class GameState` (очки прогресса `score` до 1000, счетчик брака `missedCount` до 12, список объектов, статус: PLAYING, GAME_OVER, PAUSED, WON).
- [x] 1.5. В `domain/usecases` создать `GameEngine` для игрового тика. Реализовать динамическую сложность: скорость падения объектов и шанс появления `BadPanama` должны расти в зависимости от текущего `score` (чем ближе к 1000, тем сложнее).

## Фаза 2: Игровой цикл и UI (Presentation)
- [x] 2.1. Создать `GameViewModel` с паттерном MVI (StateFlow для `GameState`, каналы для `GameEffect`). Добавить корутину для Game Loop (~60 FPS).
- [x] 2.2. Создать `GameScreen.kt` (Compose). Вывести текстовые заглушки для Score (X/1000) и Missed (Y/12).
- [x] 2.3. В `GameScreen` добавить `Canvas`. Отрисовать коробку (игрока) внизу экрана и настроить перемещение коробки (через кнопки или свайпы).
- [x] 2.4. Настроить отрисовку падающих объектов (`GameObject`) из состояния на `Canvas`.

## Фаза 3: Механика коллизий и Финал игры
- [x] 3.1. В `GameEngine` добавить логику столкновений (пересечение координат).
- [x] 3.2. Настроить очки: поймал `Panama` -> `score` +1. Пропустил `Panama` -> `missedCount` +1. Поймал `BadPanama` -> `missedCount` +1.
- [x] 3.3. Логика Game Over: если `missedCount` >= 12, статус меняется на GAME_OVER. Отрисовать UI проигрыша.
- [x] 3.4. Логика Победы: если `score` >= 1000, статус меняется на WON. Отрисовать UI победы с кнопкой "Купить на WB" (заглушка URL) и текстом промокода (заглушка "PROMO2026").

## Фаза 4: Спасение очков и Монетизация (РСЯ)
- [x] 4.1. Настроить инициализацию Yandex Mobile Ads SDK.
- [x] 4.2. Создать `AdsManager` для загрузки Rewarded Video.
- [x] 4.3. На экране Game Over добавить кнопку "Посмотреть рекламу и убрать часть брака".
- [x] 4.4. Логика: после успешного просмотра видео уменьшить `missedCount` на 4 (на 1/3 от 12), но не меньше 0. Вернуть статус PLAYING и возобновить игровой цикл.

## Фаза 5: Firebase и Сохранение прогресса
- [х] 5.1. Настроить Firebase Auth (Google Sign-In) на стартовом экране.
- [x] 5.2. Настроить Firestore. В `data/repository` создать `ProgressRepository`.
- [x] 5.3. Реализовать автосохранение: отслеживать Lifecycle Android-приложения (onPause/onStop). При сворачивании или паузе отправлять текущие `score` и `missedCount` в Firestore.
- [x] 5.4. Реализовать загрузку: при старте игры (или при входе в аккаунт) проверять базу данных и загружать последний `GameState`.

## Фаза 6: Визуал и AR-примерка
- [x] 6.1. Заменить фигуры на `Canvas` на реальные спрайты (Bitmap) от дизайнера.
- [x] 6.2. Настроить библиотеку для AR (например, ARCore + Sceneview/Filament).
- [x] 6.3. Создать отдельный экран `ARScreen.kt` с доступом к фронтальной камере.
- [x] 6.4. Реализовать трекинг лица (Augmented Faces) и привязку 3D-модели панамы (.glb/.gltf) к верхней части головы пользователя. Добавить кнопку для перехода к AR-примерке с главного экрана или экрана победы.

## Фаза 7: Сочность, AR+ и Лояльность (Polishing)
- [x] 7.1. Добавить виброотклик (Haptic Feedback) при поимке предметов и ошибках.
- [x] 7.2. Реализовать комбо-систему: всплывающие надписи и визуальные эффекты при серийных поимках без ошибок.
- [x] 7.3. Добавить простую систему частиц (Particles) на Canvas при успешном сборе панамы.
- [x] 7.4. Расширить функционал AR: водяной знак с рекордом на скриншоте, возможность крутить панаму на голове, не крутя саму голову, чтобы игрок мог увидеть как она сидит со всех сторон, так как она может носиться и на 180 градусов.
- [x] 7.5. Улучшить экран победы: добавить эффект «скретч-карты» для получения промокода и прямую кнопку «Купить», и чтобы было уведомление либо сделать скрин, либо после победы отображать промокод в приложении, а то вдруг выйдет и потеряет промокод и надо будет заново проходить.
- [x] 7.6. Реализовать интерактивный мини-туториал (обучающий режим) при первом запуске.

- [х] 7.7 недостаточно света или расположение источника, тусклая панама, полутемная как будто
- [x] 8.0 при проигрыше нужно выводить текущий счет, например проиграл на 37 панамах, нажимаю заново и пытаюсь побить рекорд, чтобы небольшое соревнование можно было устроить с друзьями
- [х] 8.1 у нас повился сервер на селектел, нужно оформить его, перенести туда промокод, настроить все таким образом, чтобы он подтверждал, что играет реальный человек и не использует софт для быстрого прохождения игры. Так же создать простейшую админку, чтобы в 1 клик можно было менять промокод за победу, так как они не вечные и каждый месяц придется его обновлять. сам сервер у нас будет использоваться для нескольких проектов, 1 там уже есть, надо, чтобы они не мешали и не конфликтовали друг с другом
- [х] 8.2 коробки на конвеере которые в наших файлах называются korob_1 - korob_5 и самый важный korob_qr_code не должны быть плоскими, они должны пропорционально уменьшаться, сейчас они из квадратных стали очень прямоугольными (плоскими) что даже qr не считывается. korob_1 - korob_5 должны рандомно выезжать и не через равное расстояние, а в рандомном количестве, например 3 подряд пропуск, затем 1, пропуск, затем 4 пропуск затем 1 и так далее, чтобы не было монотонного паравозика с равным расстоянием. Когда выезжает коробка с qr кодом она должна остановиться в конце, сейчас она это делает, но другие коробки не должны на нее наезжать, а должны остановиться рядом с рей и ждать, пока она не уедет и поехать вместе с ней. ты чуть-чуть сломал логику движения коробок. надо поправить. а так же опустить их ниже, сейчас они в воздухе выше конвера парят, скажи в какой строке я сам подберу высоту
- [х] 8.3 добавить ползунок регулировки размера панамы и высоты, и с оклюдером проблемки, на телефоне у друга появились большие засветы, не так как на моем как мы можем его адаптировать, чтобы он четко подгонялся под размер головы
- [х] 8.4 долкий тап на qr код нужно заменить на короткий, чтобы при быстром нажатии сразу переходил по ссылке
- [] 8.5 при первом запуске не на всех телефонах появляется инструкция, в тексте инструкции написано что победа при 5000 панам, надо изменить на 1000, на а50 появилась, а вот на s25 ультра уже нет.
- [] 8.6 подготовить боевой режим уже на 1000 панам, сейчас для тестов стоит 50. градация сложности соответственно должна быть по 200 очков, а не как сейчас по 10. на экране с инструкцией как играть прописано 5000 панам нужно поймать, надо исправить.
